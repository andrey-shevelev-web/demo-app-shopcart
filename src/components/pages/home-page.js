import React from 'react';

import BookList from '../book-list';
import CartTable from '../cart-table';

export default () => {
  return (
    <div> 
      <BookList />
      <CartTable />
    </div>
  );
}